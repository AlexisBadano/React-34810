import NavBar from './NavBar';

const Header = () => {
    return (
        <header>
            <h1 className="brand">Thunder Jam's</h1>
            <NavBar/>
        </header>
    )
}

export default Header